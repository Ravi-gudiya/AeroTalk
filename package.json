{
  "name": "aerotalk-monorepo",
  "private": true,
  "version": "0.2.0",
  "type": "module",
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "start": "npm start --prefix backend",
    "build": "npm run build --prefix frontend"
  }
}
